# AI World Cup Simulator

Small AI-driven World Cup simulator. Run `python run_world_cup.py` to simulate a tournament with fictional teams.

Options:

- `--teams NAME ...` supply team names (must be multiple of 4)
- `--seed N` deterministic random seed
- `--verbose` print group and knockout details
